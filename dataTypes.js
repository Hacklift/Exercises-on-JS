// I'm going to use multi-comment line to list out all the data types that JS supports.

/* Data is anything that is meaningful to the computer
JS supports about 7 different data types.They are:
1. string which simply means text.
2. number like the name implies are numbers.
3. undefined is something that has not been defines.
4. null simply means nothing.
5. boolean simply means true or false.
6. symbol is a value that is unique
7. object is an instance of a class, used to store different key value pairs.
*/

let rand = Math.random();
console.log(rand);
let rand1 = Math.floor(rand)
console.log(rand1);
let rand2 = rand1 + 10;
console.log(rand2);